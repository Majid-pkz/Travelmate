const User = require('./User');
const Trip = require('./Trip');
const TripType = require('./TripType');
const Interest = require('./Interest');
const Profile = require('./Profile');

module.exports = { User, Trip, TripType, Interest, Profile };
